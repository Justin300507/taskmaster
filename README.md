# Taskmaster

> Generated by **ForgeAI** — AI-powered full-stack application generator.

## Features

- Create, read, update, and delete tasks with title, description, due date, and completion status
- Secure sign‑up, login, and JWT‑based session management

## Tech Stack

| Layer | Technology |
|-------|-----------|
| Backend | FastAPI |
| Database | SQLite |
| Frontend | React + Vite |
| Auth | JWT Bearer tokens |

## Quick Start

### Prerequisites

- Python 3.11+
- Node.js 18+ (for frontend)

### Backend

```bash
cd app
pip install -r requirements.txt
uvicorn main:app --reload
```

API available at: `http://localhost:8000`
Interactive docs: `http://localhost:8000/docs`

### Frontend

```bash
npm install
npm run dev
```

Frontend at: `http://localhost:5173`

### Docker

```bash
docker build -t taskmaster .
docker run -p 8000:8000 taskmaster
```

## API Reference

| Method | Path | Description |
|--------|------|-------------|
| `POST` | `/auth/signup` | Signup |
| `POST` | `/auth/login` | Login |
| `GET` | `/auth/me` | Me |
| `GET` | `/invites` | List Invites |
| `GET` | `/invites/{invite_id}` | Get Invite |
| `POST` | `/invites` | Create Invite |
| `PUT` | `/invites/{invite_id}` | Update Invite |
| `DELETE` | `/invites/{invite_id}` | Delete Invite |
| `GET` | `/projects` | List Projects |
| `GET` | `/projects/{project_id}` | Get Project |
| `POST` | `/projects` | Create Project |
| `PUT` | `/projects/{project_id}` | Update Project |
| `DELETE` | `/projects/{project_id}` | Delete Project |
| `GET` | `/project_memberships` | List Project Memberships |
| `GET` | `/project_memberships/{membership_id}` | Get Project Membership |
| `POST` | `/project_memberships` | Create Project Membership |
| `PUT` | `/project_memberships/{membership_id}` | Update Project Membership |
| `DELETE` | `/project_memberships/{membership_id}` | Delete Project Membership |
| `POST` | `/seed` | Seed |
| `GET` | `/stats/summary` | Get Stats Summary |


## Database Schema

5 table(s): Invite, ProjectMembership, Project, Task, User

See [ARCHITECTURE.md](ARCHITECTURE.md) for full ER diagram.

## Project Structure

```
app/
├── main.py          # FastAPI entry point
├── database.py      # DB connection & session
├── models/          # SQLAlchemy ORM models
├── schemas/         # Pydantic request/response schemas
├── routes/          # API route handlers
├── services/        # Business logic
└── requirements.txt
src/                 # React frontend
```

---
_Built with [ForgeAI](https://github.com/forgeai) · 30 endpoints · 5 models_
